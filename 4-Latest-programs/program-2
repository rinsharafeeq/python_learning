Weather CLI App (pip + API + JSON)
Install the requests library using pip.
Use the free OpenWeatherMap API (or any simple weather API) to fetch weather for a city entered by the user.
Parse the JSON response and print temperature, humidity, and conditions.
Wrap it in a WeatherFetcher class with a method get_weather(city).

Practice: Installing external packages, making HTTP requests, parsing JSON, classes.
