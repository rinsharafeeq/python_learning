Daily Task Logger (File + datetime + try/except)
Goal:
Create a program that lets the user write a short task for the day. The program saves it to a file named tasks.txt with a timestamp.

Requirements:

Use the datetime module to get the current date and time.

Ask the user: “What is your task for today?”

Append the task to tasks.txt in this format:

text
2025-03-20 14:35:22 - Buy groceries
Use try/except to handle possible file errors (e.g., permission denied).

Bonus:
Read back all saved tasks when the program starts.

